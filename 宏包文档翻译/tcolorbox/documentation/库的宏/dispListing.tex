%%\clearpage
\begin{docEnvironment}{dispListing}{}
Creates a colored box based on a \refEnvLe{tcolorbox}.
It displays the environment content as source code.
The appearance is controlled by \refKeyLe{/tcb/documentation listing style}
and the style \refKeyLe{/tcb/docexample}. It may be
changed by redefining this style.

基于 \refEnvLe{tcolorbox} 创建一个有颜色的盒子。它将环境内容以源代码形式显示出来。外观由 \refKeyLe{/tcb/documentation listing style} 和样式 \refKeyLe{/tcb/docexample} 控制。可以通过重新定义此样式来进行更改。
\begin{dispExample}
\begin{dispListing}
This is a \LaTeX\ example.
\end{dispListing}
\end{dispExample}
\end{docEnvironment}

\begin{docEnvironment}{dispListing*}{\marg{options}}
The starred version of \refEnvLe{dispListing} takes \refEnvLe{tcolorbox} \meta{options}
as parameter. These \meta{options} are executed after \refKeyLe{/tcb/docexample}.

\refEnvLe{dispListing} 的加星版本以 \refEnvLe{tcolorbox} 的 \meta{选项} 作为参数。这些 \meta{选项} 在 \refKeyLe{/tcb/docexample} 之后执行。
\begin{dispExample}
\begin{dispListing*}{title=My listing}
This is a \LaTeX\ example.
\end{dispListing*}
\end{dispExample}
\end{docEnvironment}