\begin{docKey}
  {--value, -v}{}%
  {}%
  Show value of command instead (i.e. \cs{the}\cs{command}).

  显示命令的值，而不是命令本身（即 \cs{the}\cs{command}）。

% \begin{commandshell} 
% latexdef -c '[a4paper]{book}' -v paperwidth
% \end{commandshell}  

% \begin{commandshell} 
% latexdef -c '[letter]{book}'  -v paperwidth
% \end{commandshell}  


\begin{tcblisting}{%title={Snapshot of the staging area},
csh texdef result={
\mintinline[breaklines]{shell}|latexdef -c '[a4paper]{book}' -v paperwidth|
}
}
\the\paperwidth:
597.50787pt
\end{tcblisting}


\begin{tcblisting}{%title={Snapshot of the staging area},
csh texdef result={
\mintinline[breaklines]{shell}|latexdef -c '[letter]{book}'  -v paperwidth|
}
}
\the\paperwidth:
614.295pt
\end{tcblisting}

\begin{tcblisting}{%title={Snapshot of the staging area},
csh texdef result={
\mintinline[breaklines]{shell}|latexdef -c '[letter]{book}' -f  -v paperwidth|
}
}
\paperwidth is defined by (La)TeX.

\the\paperwidth:
614.295pt
\end{tcblisting}

\end{docKey}